#include <stdio.h>
int main(){
    int a,b,i,gcd=1;
    printf("enter two number:");
    scanf("%d %d", &a,&b);
    for(int i=1;i<=a&&i<=b;i++)
    {
        if(a%i==0 && b%i==0){
        gcd=i;
    }
    }
    printf("G.C.D=%d", gcd);
    return 0;
}
